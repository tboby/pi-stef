import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function catalogExtension(_pi: ExtensionAPI): void {
  // Extension entry point – tools will be registered in later stories.
}
